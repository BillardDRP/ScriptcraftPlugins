'use strict'
    // fetch databases
var Drone = require('drone'),
    blocks = require('blocks')
    sounds = require('sounds');
// function start
    function EntBattlements(amount) {
      // customization start
        if (typeof amount=='undefined'){
          amount=1
        }
      // End customization, start function
        while(amount!=1){
          for(){

          }

    }



 Drone.extend(EntBattlements)
