'use strict'
// fetch libs
var Drone = require('drone'),
    blocks = require('blocks'),
    inventory = require('inventory'),
    items = require('items'),
    utils = require('utils'),
    sounds = require('sounds'),
    events = require('events');

    var spawns = 0
  // credits on first spawn

      while (spawns==0) {
        function spawnin(event) {
          echo(pstats plugin loaded, version b1)
          echo(Created by Diego Eann)
          spawns++;
      };
    };
    events.playerRespawned(spawnin);

    /* Stat list
     */

// call function in chat
    function pstats(){
      echo
    }


    function keepstatdeaths(event){
       var playerdeaths++
    };
      events.playerDeath(keepstatdeaths);
