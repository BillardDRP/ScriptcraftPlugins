'use strict'
// global vars
var Drone = require('drone'),
    blocks = require('blocks'),
    inventory = require('inventory'),
    items = require('items'),
    utils = require('utils'),
    sounds = require('sounds'),
    events = require('events');
function challenge6(event) {
  var Target = player.event;
  var blockcount = 0;
        blockcount++
        this.HouseEnt();
        echo(/*player.event,*/ "You have destroyed" 'blockcount' "blocks.");
};
events.blockDestroy(challenge6)
